{
  "name": "unplugin-declare",
  "description": "Unplugin to generate type declaration for ts(x) libraries.",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "review": "prettier --check . && oxlint"
  },
  "devDependencies": {
    "oxlint": "^1.14.0",
    "prettier": "^3.6.2",
    "prettier-plugin-sh": "^0.18.0"
  }
}
